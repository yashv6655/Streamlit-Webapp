## Streamlit Machine Learning Webapp

---

### Packages Used

- Scikit-Learn
- Streamlit
- Matplotlib

---

### Install packages

`pip install streamlit scikit-learn matplotlib`
: If a `numpy` error crashes the application, try running `pip install numpy==1.19.3`
